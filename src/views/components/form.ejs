<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <form action="/components/" method="post">
      <input name="textfield" type="text" value="test" />
      <input name="textfield" type="text" value="test1" />
      <input name="textfield" type="text" value="test2" />
      <input name="textfield" type="text" value="test3" />

      <button type="submit">submit</button>
    </form>
  </body>

  <script>
    const form = document.querySelector("form");
    form.addEventListener("submit", (e) => {
      e.preventDefault();
      const formData = new FormData(form);
      const formString = new URLSearchParams(formData).toString();
      console.log(form.children);
    });
  </script>
</html>
